# HD-021 · 跨平台helper构建与部署

状态：`planned`。阶段：P3。预计有效人日：2–4。

## 目标与范围

多架构bridge产物、显式安装确认、校验、原子更新。

拟建/修改产物：`bridge/release/`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-008, HD-020

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC25**：安装需同意；平台架构正确；签名/可信hash检查；临时写入和原子替换；不使用sudo。

## 风险与回滚

风险：错架构/篡改/静默安装。

回滚：保留旧helper，禁用新连接。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
