# HerdDesk.Infrastructure.Tests

BCL console runner for configuration atomic write/backup/restore, diagnostic privacy, owned child process, RPC request/subscription (fake `--fake-bridge` child), HD-013 `TerminalCliTransport` (fake `--fake-terminal` child), HD-020 L1 SSH parser/trust/test (fake process runner / `--fake-ssh`), and HD-021 L1 helper planner/publisher (fake filesystem / fake ssh -T). Uses temp directories. Confirmed control takeover may add `--takeover`; default observe/control argv still omits it. L2 named-pipe ACL, live herdr, and isolated OpenSSH remain UNVERIFIED. `--fake-bridge` / `--fake-terminal` / `--fake-ssh` are test-host modes, not product CLIs. SchemaV1 decoder fail-closed cases live in Contract tests.
