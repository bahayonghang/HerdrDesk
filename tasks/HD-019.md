# HD-019 · 本地MVP综合验收

状态：`planned`。阶段：P2。预计有效人日：2–3。L1 catalog 与 composition 已落地；L2/L3 live 仍为 UNVERIFIED。产品 AC06/AC07/AC10/AC15 未通过。

## 目标与范围

三类agent文本TUI、PowerShell、窗口关闭与错误注入。

拟建/修改产物：`evidence/local-mvp/`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-012, HD-015, HD-017, HD-018

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC06**：一次输入仅一次到达；resize实际生效；release结束桥但pane继续运行。
- **AC07**：两个observer并存，第二controller不takeover时不夺权；确认takeover后旧控制者停止写入。
- **AC10**：Claude Code、Codex、OpenCode各记录实际版本；导航、确认、取消、输入、滚动通过；图形另列。
- **AC15**：关闭/崩溃GUI后herdr daemon与已有agent继续；不调用server stop或杀进程树中的daemon。

## 风险与回滚

风险：脚本通过但真实TUI失败。

回滚：标注具体不支持agent/模式，禁止发布对应能力。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
