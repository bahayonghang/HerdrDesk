# HD-017 · workspace与agent基本操作

状态：`planned`。阶段：P2。预计有效人日：1–2。

## 目标与范围

从schema验证创建/重命名/关闭和agent能力；默认保留审批。

拟建/修改产物：`HerdDesk.Core/Commands`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-009, HD-016

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC20**：创建/关闭/重命名按实际schema验证；超时后查询再决定，不盲重试非幂等操作。

## 风险与回滚

风险：非幂等操作重复与破坏误触。

回滚：关闭创建/删除按钮，保留浏览。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
