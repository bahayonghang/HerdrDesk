# HD-016 · 控制权状态机

状态：`planned`。阶段：P2。预计有效人日：1–2。

## 目标与范围

显式申请、busy、takeover确认、release、断线回到observe。

拟建/修改产物：`HerdDesk.Core/TerminalLease`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-013, HD-014

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC07**：两个observer并存，第二controller不takeover时不夺权；确认takeover后旧控制者停止写入。
- **AC14**：断网前后排队的敏感输入不被自动重发；状态明确显示未发送或结果未知。
- **AC16**：默认不takeover、不自动批准、不默默开启agent bypass；破坏操作有目标确认。

## 风险与回滚

风险：抢占/串写/输入重放。

回滚：所有pane强制只读，清空未发送队列。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
