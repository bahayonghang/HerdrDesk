# HD-033 · 性能、可访问性与soak

状态：`planned`。阶段：P5。预计有效人日：2–4。

## 目标与范围

按统一负载测性能/资源/8h稳定性，修复关键问题。

拟建/修改产物：`evidence/quality/`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-026, HD-032

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC27**：超长行、慢renderer、高输出后内存有界；不丢delta维持假正常；取消可终止本进程。
- **AC28**：按docs/10统一硬件与负载实测冷启动、内存、CPU、输入到呈现p95并留原始数据。
- **AC29**：连接/关闭pane视图100次后桥进程和句柄回到基线容差；无持续增长。
- **AC37**：键盘与屏幕阅读器可完成搜索、控制、释放、关闭确认；颜色不是唯一状态编码。
- **AC38**：100/150/200%及多显示器、深浅色、高对比通过；无持续resize抖动。
- **AC46**：8小时代表负载及100次断连/切换通过；失败注入不破坏pane存活。

## 风险与回滚

风险：以平均值掩盖长尾与资源增长。

回滚：降低并发/关闭实验renderer。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
