# HerdDesk.Infrastructure.Tests

BCL console runner for configuration atomic write/backup/restore, diagnostic privacy, owned child process, RPC request/subscription (fake `--fake-bridge` child), HD-013 `TerminalCliTransport` (fake `--fake-terminal` child), HD-020 L1 SSH parser/trust/test (fake process runner / `--fake-ssh`), HD-021 L1 helper planner/publisher (fake filesystem / fake ssh -T), HD-022 L1 remote session transport (fake `--fake-ssh-channel` children), HD-024 L1 SSH failure classifier / recovery block store, and HD-025 L1 `SshConnectionLease` (fake `--fake-ssh-channel` children, injected admission budget). HD-027 L1 Windows reserved-name mapping-required and codec wrap/cancel flags. HD-028 L1 `LocalFileEndpoint` on disposable temp roots (no Integration.Windows). HD-031 L1 `AttachmentCache` TTL/capacity/lease/exact-id cleanup on a disposable cache root, plus `WindowsClipboardSnapshotReader` with an injected capture (no live user clipboard, no watcher). Uses temp directories. Confirmed control takeover may add `--takeover`; default observe/control argv still omits it. L2 named-pipe ACL, live herdr, isolated OpenSSH, live SSH, and filebridge FS remain UNVERIFIED. `--fake-bridge` / `--fake-terminal` / `--fake-ssh` / `--fake-ssh-channel` are test-host modes, not product CLIs. SchemaV1 decoder fail-closed cases live in Contract tests.
