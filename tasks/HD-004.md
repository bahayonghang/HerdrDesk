# HD-004 · 终端桥协议探针

状态：`blocked`。阶段：G0。预计有效人日：2–3。

## 目标与范围

在隔离pane测试observe/control/resize/release/EOF并采样失败行为。

拟建/修改产物：`tests/fixtures/real-terminal-v082/`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-001

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC05**：observe获得可解码完整基线；seq/尺寸/Base64正确；EOF被报告而非冒充pane退出。

## 风险与回滚

风险：源码支持但Windows运行路径失败。

回滚：只读模式；保留本地诊断，不启动完整GUI开发。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。

## 2026-09-07 实施记录

离线探针与合成回归已完成；等待HD-001运行证据及隔离Windows pane。

详见 `docs/implementation-g0.md`；该任务AC尚未通过。
