# HD-008 · RPC stdio bridge和请求层

状态：`planned`。阶段：P1。预计有效人日：2–3。

## 目标与范围

实现平台socket字节桥、request id、异步读写和取消。

拟建/修改产物：`bridge/ 与 HerdDesk.Infrastructure/Rpc`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-003, HD-007

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC03**：default/named session、显式path、Unicode路径各连到预期API endpoint；错误用户/权限可诊断。
- **AC04**：snapshot/订阅及required字段依据实际schema，通过error/unknown字段fixture。

## 风险与回滚

风险：stderr污染或读写互锁。

回滚：回退snapshot诊断，不冒充实时连接。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
