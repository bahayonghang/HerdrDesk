# HD-001 · 锁定上游事实与版本

状态：`in_progress`。阶段：G0。预计有效人日：0.5–1。

## 目标与范围

记录 tag、blob、schema、二进制 hash；整理 preview 与 stable 差异。

拟建/修改产物：`evidence/compatibility-baseline.json`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

无任务依赖；仍须遵循对应阶段入口条件。

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC01**：本地/远端各记录 CLI version、daemon ping、schema protocol与hash；不把客户端schema当服务器证明。

## 风险与回滚

风险：错误版本导致所有后续fixture失效。

回滚：停用自动兼容判断，恢复只读诊断。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。

## 2026-09-07 实施记录

源码commit/blob/schema header已复核；缺运行中本地与远端证据。

详见 `docs/implementation-g0.md`；该任务AC尚未通过。
