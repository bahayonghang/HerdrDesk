# HerdDesk.Core.Tests

BCL console runner (`dotnet run`，不是 `dotnet test`). Fake ports only. No WinUI/herdr/SSH. HD-009 mapper/Store tests use decoded Contracts inputs and must not load Infrastructure. HD-010 DeviceSession/race tests drive the shipped actor from real start state with fake HD-008 ports, barriers, and an injected `TimeProvider`. HD-012 Attention tests drive the shipped reducer from a real start state (baseline, reconnect, dedup, unread, stale/unknown). HD-016 ControlLeaseCoordinator tests drive the shipped actor from a real start state with fake HD-013 events. HD-017 ResourceCommandCoordinator tests drive capability/freshness/confirmation/timeout gates with fake RPC/Store. HD-018 Recovery tests drive RecoveryPolicy, DeviceSession retry, and lease stale/observe signals with barriers and fake time. HD-024 tests drive equal-jitter backoff, per-SessionKey isolation, and auth/host-key zero-retry with fake clock/RNG. HD-019 L1 composition tests drive DeviceSession + ControlLeaseCoordinator + InputPolicy + RecoveryPolicy + ResourceCommandCoordinator + AttentionReducer from a real start state with fakes. HD-023 L1 aggregation tests drive `GlobalProjectionStore` / `GlobalTargetResolver` / search p95 / old-epoch isolation on in-memory fake Stores (no network, no terminal spawn). HD-025 L1 admission/queue/dirty-set tests drive `ConnectionAdmissionPolicy`, `TerminalQueueBudget`, and `DirtySetBudget` with injected budgets (no sleep, no live SSH). HD-028 L1 `TransferCoordinator` tests drive Local↔Local fakes, KeepBoth/Fail, lease cap, and replace observation (no live SSH). L2 live subscribe interleave remains UNVERIFIED. HD-012 Windows toast L2 remains UNVERIFIED. HD-016 L2 live lease remains UNVERIFIED. HD-017 L2 live mutation remains UNVERIFIED. HD-018 L2 live disconnect remains UNVERIFIED. HD-019 L2 live local MVP and L3 IME/TUI remain UNVERIFIED. HD-023 L2 live 3-device p95 remains UNVERIFIED. HD-024 L2 live auth remains UNVERIFIED. HD-025 L2 live SSH/perf remains UNVERIFIED.
