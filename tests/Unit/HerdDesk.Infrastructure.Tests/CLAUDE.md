# HerdDesk.Infrastructure.Tests

BCL console runner for configuration atomic write/backup/restore, diagnostic privacy, owned child process, and RPC request/subscription (fake `--fake-bridge` child). Uses temp directories. L2 named-pipe ACL remains UNVERIFIED. `--fake-bridge` is a test-host mode, not a product CLI. SchemaV1 decoder fail-closed cases live in Contract tests.
