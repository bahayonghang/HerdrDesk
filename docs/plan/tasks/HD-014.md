# HD-014 · 可交付WebView2终端adapter

状态：`planned`。阶段：P2。预计有效人日：3–5。

## 目标与范围

打包本地xterm、字节写入、在途窗口、origin校验。

拟建/修改产物：`HerdDesk.Terminal.Web/ 与 web/terminal/`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-005, HD-007, HD-013

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC08**：中文/emoji/组合字符跨块拆分后无替换字符、无重复；bytes模式可准确传输控制键。
- **AC27**：超长行、慢renderer、高输出后内存有界；不丢delta维持假正常；取消可终止本进程。

## 风险与回滚

风险：混合渲染通道注入/无界缓冲。

回滚：终止当前视图并从observe重建。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
