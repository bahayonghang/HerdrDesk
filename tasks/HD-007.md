# HD-007 · 仓库骨架与CI

状态：`planned`。阶段：P1。预计有效人日：1–2。

## 目标与范围

创建模块边界、锁定依赖、配置分析器和测试流水线。

拟建/修改产物：`src/ 与 .github/workflows/`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-006

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC39**：精确依赖锁定、SDK记录、干净机还原成功；不写伪造package version。
- **AC40**：format/analyzers/unit/contract失败会阻断合并；synthetic fixture与real fixture分开。
- **AC47**：Core/Contracts不依赖WinUI/WebView2/SSH实现；业务可用fake transport单测。

## 风险与回滚

风险：UI/协议耦合，依赖无法还原。

回滚：先保留Contracts/Core与离线测试。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
