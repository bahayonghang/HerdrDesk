# HD-034 · 签名、安装与更新回滚

状态：`planned`。阶段：P5。预计有效人日：2–3。

## 目标与范围

MSIX/运行时依赖、签名校验、版本降级与配置迁移。

拟建/修改产物：`packaging/`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-007, HD-021

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC41**：干净受支持Windows可安装启动卸载；WebView2/runtime依赖、Publisher身份明确。
- **AC42**：更新签名验证和失败回滚通过；配置兼容；不强制停止/升级herdr。

## 风险与回滚

风险：升级杀掉任务或配置不可回滚。

回滚：保留前一签名版本和配置备份。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
