# HerdDesk.Infrastructure.Tests

BCL console runner for configuration atomic write/backup/restore, diagnostic privacy, owned child process, RPC request/subscription (fake `--fake-bridge` child), HD-013 `TerminalCliTransport` (fake `--fake-terminal` child), HD-020 L1 SSH parser/trust/test (fake process runner / `--fake-ssh`), HD-021 L1 helper planner/publisher (fake filesystem / fake ssh -T), HD-022 L1 remote session transport (fake `--fake-ssh-channel` children), HD-024 L1 SSH failure classifier / recovery block store, and HD-025 L1 `SshConnectionLease` (fake `--fake-ssh-channel` children, injected admission budget). Uses temp directories. Confirmed control takeover may add `--takeover`; default observe/control argv still omits it. L2 named-pipe ACL, live herdr, isolated OpenSSH, and live SSH remain UNVERIFIED. `--fake-bridge` / `--fake-terminal` / `--fake-ssh` / `--fake-ssh-channel` are test-host modes, not product CLIs. SchemaV1 decoder fail-closed cases live in Contract tests.
