# HerdDesk.Core.Tests

BCL console runner (`dotnet run`，不是 `dotnet test`). Fake ports only. No WinUI/herdr/SSH. HD-009 mapper/Store tests use decoded Contracts inputs and must not load Infrastructure.
