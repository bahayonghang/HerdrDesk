# real-terminal-v082

Placeholder for desensitized herdr v0.8.2 terminal-session captures.

No authorized isolated pane or live herdr grant exists for this tree. `captures` in `index.json` is empty. This directory is not a successful runtime corpus.

Label:

- `tests/fixtures/lease-cases.json` is **synthetic**.
- Files under this directory would be **real** captures. None are present.

Do not commit terminal text, credentials, or private paths. Do not copy synthetic frames here and mark AC05 passed.
