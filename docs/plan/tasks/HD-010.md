# HD-010 · 订阅与收敛策略

状态：`planned`。阶段：P1。预计有效人日：2–3。

## 目标与范围

订阅确认+snapshot+dirty重读；有限合并和周期校准。

拟建/修改产物：`HerdDesk.Core/DeviceSession`。这是目标仓库路径，不表示本包已包含实现。

## 前置依赖

HD-009

## 执行要求

先读取相关ADR与原始证据，明确测试fixture是合成还是真机；先建立/确认本任务记录再修改代码。采用最小可验证改动，业务逻辑不绕过Core策略；新增行为需配测试。涉及远端、输入、删除、部署的操作必须使用隔离测试资源。

## 验收

- **AC12**：订阅与snapshot交错期间创建/关闭/改状态后，最终投影收敛，旧epoch消息不回写。

## 风险与回滚

风险：snapshot/event竞态产生幽灵pane。

回滚：关闭增量投影，短期全量校准并标记降级。不得以停止用户herdr会话代替应用级回滚。

## 完成证据

记录实际commit、测试命令/版本、通过与失败结果、人工验收截图或脱敏日志、未覆盖项。仅静态检查或mock通过不能标记真实Windows行为passed；未满足验收保留blocked并说明原因。
